#include <stdio.h>
/**
 * main - Entry point for the program
 * Return: Always 0 (success)
 */

int main(void)
{
	char C;

	C  = 'a';
	while
		(C <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
